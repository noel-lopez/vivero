## Agent skills

### Issue tracker

Issues live in GitHub Issues for `noel-lopez/vivero`, via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Canonical triage roles, default vocabulary (label string = role name). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
